{
  "name": "@bruff/utils",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "type": "module",
  "main": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./dom": "./dom.ts"
  },
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint",
    "test:watch": "vitest",
    "test": "pnpm run test:chromium && pnpm run test:firefox && pnpm run test:webkit && pnpm run test:node",
    "test:node": "vitest run --config vitest.node.config.ts --coverage",
    "test:chromium": "vitest run --browser=chromium --coverage ",
    "test:firefox": "vitest run --browser=firefox",
    "test:webkit": "vitest run --browser=webkit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@bruff/eslint-config": "workspace:0.0.0",
    "@vitest/browser": "catalog:",
    "@vitest/browser-playwright": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "eslint": "catalog:",
    "playwright": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
